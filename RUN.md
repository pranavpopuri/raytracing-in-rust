# How to Run RusTX
1. Clone the repository
```sh
git clone https://github.com/pranavpopuri/raytracing-in-rust.git
```

2. Run in **RELEASE** mode for performance!
```sh
cargo r --release
```

3. There are optional command line arguments, use `--help` to see them:
```sh
cargo r --release -- --help
```